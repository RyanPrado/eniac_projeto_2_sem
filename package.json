{
  "name": "eniac-projeto",
  "version": "1.0.0",
  "description": "Projeto para a faculdade ENIAC 2º Semestre",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "release": "npx semantic-release",
    "build:clear": "rm -rf build/ROOT",
    "build:copy": "cp -r target/${npm_package_name}/ build/ROOT",
    "build": "MAVEN_OPTS=\"-XX:ErrorFile=logs/maven/hs_err_pid%p.log\" mvn -q clean package && npm run build:clear && npm run build:copy",
    "dev:mvn": "MAVEN_OPTS=\"-XX:ErrorFile=logs/maven/hs_err_pid%p.log\" mvn -q compile war:exploded && npm run build:clear && npm run build:copy",
    "dev": "concurrently \"npx dotenv -e .env -- nodemon --watch src --ext java,html,css,js,jsp,tag,xml --exec 'npm run dev:mvn'\""
  },
  "author": "Ryan Prado",
  "license": "MIT",
  "dependencies": {
    "autoprefixer": "^10.4.21",
    "nodemon": "^3.0.2"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^12.0.0",
    "concurrently": "^9.2.1",
    "dotenv-cli": "^10.0.0",
    "semantic-release": "^25.0.1"
  }
}
